version: '3.8'
services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./bot:/usr/src/app:rw
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    command: ["python3", "matching.py"]
    tty: true
