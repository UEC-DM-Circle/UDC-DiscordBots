version: '3.8'
services:
  bot:
    build: .
    volumes:
      - ./bot:/usr/src/app:rw
    restart: unless-stopped
    environment:
      - TOKEN=${TOKEN}
      - DISCORD_INFO_CHANNEL_ID=${DISCORD_INFO_CHANNEL_ID}
      - DISCORD_NEWCARD_CHANNEL_ID=${DISCORD_NEWCARD_CHANNEL_ID}
      - DISCORD_RESULT_CHANNEL_ID=${DISCORD_RESULT_CHANNEL_ID}
      - DB_HOST=${DB_HOST}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    command: ["python3", "information.py"]
    tty: true
